function printArrayElements(arr) {
    for (let i = 0; i < arr.length; i++) {
      console.log(arr[i]);
    }
    }
    

    const names = ["ankit", "amar", "kishan", "ankur"];
    printArrayElements(names);